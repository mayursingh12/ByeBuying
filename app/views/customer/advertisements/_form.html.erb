<%= simple_form_for [:customer, @advertisement] do |f| %>
    <%= f.input :title %>
    <%= f.input :category_id, collection: category_for_collection, prompt: 'Select Category' %>
    <%= f.input :contact %>
    <%= f.input :email %>
    <%= f.input :description%>
    <%= f.submit 'Save', class: 'btn btn-success' %>
<% end %>