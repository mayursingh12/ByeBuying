<div class="row">
  <div class="col-sm-4 col-sm-offset-4">
    <h2>Enquiry for
      <%= @product.name unless @service.present? %>
      <%= @service.service_description unless @product.present? %>
    </h2>
    <%= render partial: 'form' %>
  </div>
</div>
